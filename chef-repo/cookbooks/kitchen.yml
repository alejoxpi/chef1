---
driver:
  name: vagrant

provisioner:
  name: chef_solo
  cookbooks_path: './'

verifier:
  name: inspec

platforms:
  - name: ubuntu-18.04
  #- name: centos-8

suites:
  - name: my
    run_list:      
      #- recipe[line::default]
      #- recipe[apparmor::default]
      - recipe[apache2::default]
    verifier:
      inspec_tests:
        - path: apache2/test/integration/default
    attributes: