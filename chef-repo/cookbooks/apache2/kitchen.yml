---
driver:
  name: vagrant

provisioner:
  name: chef_solo

verifier:
  name: inspec

platforms:
  - name: ubuntu/bionic64
  #- name: centos-8

suites:
  - name: default
    run_list:
      - recipe[recipes/default]
    verifier:
      inspec_tests:
        - test/integration/default
    attributes: